import type { ListeningItem } from '../types'

/**
 * The listening-comprehension bank. Gutted alongside the deck rebuild — see
 * curriculum.md. Items need a broader vocabulary than the current 10-card
 * deck provides, so this is empty until the deck grows back out.
 */
export const LISTENING_ITEMS: ListeningItem[] = []
